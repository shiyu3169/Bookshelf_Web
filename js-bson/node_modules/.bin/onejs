#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../one/bin/onejs" "$@"
  ret=$?
else 
  node  "$basedir/../one/bin/onejs" "$@"
  ret=$?
fi
exit $ret
